import { ToogleBtnSwitchDirective } from './toogle-btn-switch.directive';

describe('ToogleBtnSwitchDirective', () => {
  it('should create an instance', () => {
    const directive = new ToogleBtnSwitchDirective();
    expect(directive).toBeTruthy();
  });
});

import { ToogleBtnDirective } from './toogle-btn.directive';

describe('ToogleBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new ToogleBtnDirective();
    expect(directive).toBeTruthy();
  });
});
